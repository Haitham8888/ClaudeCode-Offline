{
  "$schema": "https://json.schemastore.org/mcp",
  "description": "MCP configuration for Claude Code offline browser debugging",
  "instructions": "Place as .mcp.json in project root, or merge mcpServers into ~/.claude/settings.json",
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "@playwright/mcp",
        "--headless"
      ],
      "description": "Microsoft Playwright MCP - browser automation with console & network capture (recommended)"
    },
    "agent-browser": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "agent-browser"
      ],
      "description": "Vercel Labs agent-browser - text-first accessibility tree browser tool"
    }
  }
}
